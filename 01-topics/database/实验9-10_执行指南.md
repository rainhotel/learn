# 实验9+10 题目执行指南

## 题目1：创建数据库 StudentCourse1 及 3 个表

### 执行 SQL

```sql
IF EXISTS (SELECT name FROM sys.databases WHERE name = 'StudentCourse1')
    DROP DATABASE StudentCourse1
GO
CREATE DATABASE StudentCourse1
GO
USE StudentCourse1
GO

-- 学生表 Student1（PRIMARY KEY, UNIQUE, CHECK, NOT NULL, DEFAULT）
CREATE TABLE Student1 (
    学号     CHAR(6)         PRIMARY KEY,
    姓名     VARCHAR(20)     NOT NULL CONSTRAINT sname_unique UNIQUE,
    性别     CHAR(2)         NOT NULL CONSTRAINT ssex_Check CHECK (性别 = '男' OR 性别 = '女'),
    出生时间  SMALLDATETIME   NULL,
    总学分    INT             CONSTRAINT tot_Credit_Check CHECK (总学分 > 0),
    备注     TEXT            NULL
)
GO
ALTER TABLE Student1 ADD CONSTRAINT DF_Student1_性别 DEFAULT '男' FOR 性别
GO

-- 课程表 Course（PRIMARY KEY, CHECK 学期1~8, CHECK 学时>0, CHECK 学分>0）
CREATE TABLE Course (
    课程号   CHAR(4)         PRIMARY KEY,
    课程名   VARCHAR(40)     NOT NULL,
    开课学期  INT             NOT NULL CONSTRAINT Sem_Check CHECK (开课学期 BETWEEN 1 AND 8),
    学时     INT             NOT NULL CONSTRAINT Tn_Check CHECK (学时 > 0),
    学分     REAL            NOT NULL CONSTRAINT Cno_Check CHECK (学分 > 0)
)
GO

-- 选课表 StuCourse（复合主键, 两个外键, CHECK 成绩0~100）
CREATE TABLE StuCourse (
    学号     CHAR(6)         NOT NULL,
    课程号   CHAR(4)         NOT NULL,
    成绩     INT             NULL CONSTRAINT Score_Check CHECK (成绩 BETWEEN 0 AND 100),
    CONSTRAINT PK_StuCourse PRIMARY KEY (学号, 课程号),
    CONSTRAINT FK_StuCourse_Student1 FOREIGN KEY (学号) REFERENCES Student1(学号),
    CONSTRAINT FK_StuCourse_Course   FOREIGN KEY (课程号) REFERENCES Course(课程号)
)
GO
```

### 期望结果

> 3 张表全部创建成功，可在对象资源管理器中展开 `StudentCourse1` → 表 查看。

---

## 题目2：修改 Course 表 Tn_Check 约束（学时 ≥0 且 ≤120）

> 注：docx 中误写为"Tm_Check"，实际约束名为建表时的 `Tn_Check`。

### 执行 SQL

```sql
USE StudentCourse1
GO

-- 删除旧约束
ALTER TABLE Course DROP CONSTRAINT Tn_Check
GO

-- 添加新约束：学时 0~120
ALTER TABLE Course ADD CONSTRAINT Tn_Check CHECK (学时 >= 0 AND 学时 <= 120)
GO

-- 验证：插入学时=0（应成功）
INSERT INTO Course (课程号, 课程名, 开课学期, 学时, 学分)
VALUES ('C001', '测试课程', 1, 0, 2)
GO

-- 验证：插入学时=121（应失败）
INSERT INTO Course (课程号, 课程名, 开课学期, 学时, 学分)
VALUES ('C002', '测试课程2', 1, 121, 2)
GO

-- 清理
DELETE FROM Course WHERE 课程号 = 'C001'
GO
```

### 期望结果

| 操作 | 期望 |
|------|------|
| 学时=0 的插入 | **成功** |

学时=121 的插入 | **失败**，报错：`INSERT 语句与 CHECK 约束"Tn_Check"冲突`

---

## 题目2-补充：完成思考与练习 1 和 2

> 指实验文档末尾"思考与练习"中的两道题：记录运行结果并分析。

### 执行 SQL（先插入基础数据，再测试触发器）

```sql
USE StudentCourse1
GO

-- 插入基础数据
INSERT INTO Student1 (学号, 姓名, 性别, 出生时间, 总学分)
VALUES ('070110', '测试学生', '男', '2000-01-01', 10)
GO
INSERT INTO Course (课程号, 课程名, 开课学期, 学时, 学分)
VALUES ('2001', '数据库原理', 3, 64, 4)
GO

-- 第一次 INSERT（此时触发器不存在，应成功）
INSERT INTO StuCourse VALUES ('070110', '2001', 70)
GO

-- 创建触发器
IF EXISTS (SELECT name FROM sysobjects WHERE name = 'SC_trg' AND type = 'TR')
    DROP TRIGGER SC_trg
GO
CREATE TRIGGER SC_trg ON StuCourse
FOR INSERT
AS
    IF (SELECT 学号 FROM inserted) IN (SELECT 学号 FROM Student1)
    BEGIN
        RAISERROR ('插入操作违背数据的一致性。', 16, 1)
        ROLLBACK TRANSACTION
    END
GO

-- 第二次 INSERT（触发器已存在，应失败）
INSERT INTO StuCourse VALUES ('070110', '2001', 80)
GO

SELECT * FROM StuCourse
GO
```

### 期望结果

| 操作 | 期望 |
|------|------|
| 第一次 INSERT | **成功**，StuCourse 中有 1 行 (`070110`, `2001`, `70`) |
| 创建触发器 | 成功 |
| 第二次 INSERT | **失败**，报错 `插入操作违背数据的一致性。` |

**分析要点**（填入思考与练习）：

- 练习1：第一次 INSERT 成功因为此时尚无触发器限制；创建触发器后，学号 `070110` 在 Student1 表中存在，触发器检测到并执行 RAISERROR + ROLLBACK。
- 练习2：该触发器逻辑是若插入的学号在学生表中存在，则拒绝插入并回滚，本质是防止已存在学生重复选课。

---

## 题目2-附加①：Course 表学分字段增加默认值 2

### 执行 SQL

```sql
USE StudentCourse1
GO

-- 添加默认值约束
ALTER TABLE Course ADD CONSTRAINT DF_Course_学分 DEFAULT 2 FOR 学分
GO

-- 验证：插入时不指定学分，自动为2
INSERT INTO Course (课程号, 课程名, 开课学期, 学时)
VALUES ('C099', '默认学分测试', 1, 32)
GO
SELECT * FROM Course WHERE 课程号 = 'C099'
GO
DELETE FROM Course WHERE 课程号 = 'C099'
GO

-- 查看该表所有约束
EXEC sp_helpconstraint 'Course'
GO
```

### 期望结果

| 操作 | 期望 |
|------|------|
| 不指定学分插入 | **成功**，学分自动为 `2` |
| `sp_helpconstraint` | 输出中可见 `DF_Course_学分`，定义显示 `DEFAULT 2` |

---

## 题目2-附加②：StuCourse 表成绩约束改为 0~100

### 执行 SQL

```sql
USE StudentCourse1
GO

-- 删除旧约束
ALTER TABLE StuCourse DROP CONSTRAINT Score_Check
GO

-- 重新添加，范围 0~100
ALTER TABLE StuCourse ADD CONSTRAINT Score_Check CHECK (成绩 >= 0 AND 成绩 <= 100)
GO

-- 验证：成绩=-1（应失败）
INSERT INTO StuCourse VALUES ('070110', '2001', -1)
GO

-- 验证：成绩=101（应失败）
INSERT INTO StuCourse VALUES ('070110', '2001', 101)
GO

-- 验证：成绩=0（应成功）
INSERT INTO StuCourse VALUES ('070110', '2001', 0)
GO
DELETE FROM StuCourse WHERE 学号 = '070110' AND 课程号 = '2001' AND 成绩 = 0
GO

-- 查看约束内容
EXEC sp_helpconstraint 'StuCourse'
GO
```

### 期望结果

| 操作 | 期望 |
|------|------|
| 成绩=-1 | **失败**，`INSERT 语句与 CHECK 约束"Score_Check"冲突` |
| 成绩=101 | **失败**，同上 |
| 成绩=0 | **成功** |
| `sp_helpconstraint` | `Score_Check` 定义显示 `([成绩]>=(0) AND [成绩]<=(100))` |

---

## 题目3：对 6 类数据约束进行验证

> 首先确认基础数据存在。
>
> ```sql
> USE StudentCourse1
> GO
> SELECT * FROM Student1
> SELECT * FROM Course
> SELECT * FROM StuCourse
> GO
> ```

---

## 一、实体完整性

### (1) PRIMARY KEY 约束

#### ① 违反约束：插入主码重复的记录

```sql
USE StudentCourse1
GO
INSERT INTO Student1 (学号, 姓名, 性别, 出生时间, 总学分)
VALUES ('070110', '重复PK', '女', '2000-02-02', 20)
GO
```

期望：**失败**，`违反了 PRIMARY KEY 约束`（学号 '070110' 已存在）

#### ② 违反约束：插入主码为空的记录

```sql
USE StudentCourse1
GO
INSERT INTO Student1 (学号, 姓名, 性别, 出生时间, 总学分)
VALUES (NULL, '主码为空', '男', '2000-07-07', 10)
GO
```

期望：**失败**，`不能将值 NULL 插入列 '学号'`

#### ③ 满足约束：插入主码不重复的记录

```sql
USE StudentCourse1
GO
INSERT INTO Student1 (学号, 姓名, 性别, 出生时间, 总学分)
VALUES ('070200', '新学生', '女', '2001-01-01', 15)
GO
SELECT * FROM Student1 WHERE 学号 = '070200'
GO
```

期望：**成功**，查询到 `070200 新学生 女 2001-01-01 15`

---

## 二、参照完整性

### (2) FOREIGN KEY 约束

#### ① 外码为 NULL 的情况

```sql
USE StudentCourse1
GO
-- 成绩字段可为NULL，插入时成绩不指定
INSERT INTO StuCourse (学号, 课程号)
VALUES ('070200', '2001')
GO
SELECT * FROM StuCourse WHERE 学号 = '070200' AND 课程号 = '2001'
GO
```

期望：**成功**，成绩列显示 `NULL`（StuCourse 中仅成绩字段可为 NULL，学号和课程号是外码也是主键所以 NOT NULL。此处验证的是可为NULL的列的 NULL 情况。）

> 如果题目要求的是外码列本身为 NULL：StuCourse 的学号和课程号既是外码又是主键，不可为 NULL。可改用 UPDATE 将已有记录的 成绩（也是外码关联的一部分）验证。这里提供 UPDATE 方式的备选：
>
> ```sql
> USE StudentCourse1
> GO
> -- 查看当前 StuCourse 数据
> SELECT * FROM StuCourse
> GO
> -- 将成绩 UPDATE 为 NULL（成绩可为NULL，外码学号+课程号不变）
> UPDATE StuCourse SET 成绩 = NULL WHERE 学号 = '070200' AND 课程号 = '2001'
> GO
> SELECT * FROM StuCourse WHERE 学号 = '070200'
> GO
> ```
>
> 期望：**成功**，成绩变为 `NULL`，说明外码值不变的情况下，成绩字段允许 NULL。

#### ② 违反约束：外码取值不在被参照主码范围内

```sql
USE StudentCourse1
GO
-- 学号'999999'在Student1表中不存在
INSERT INTO StuCourse VALUES ('999999', '2001', 80)
GO
```

```sql
USE StudentCourse1
GO
-- 课程号'9999'在Course表中不存在
INSERT INTO StuCourse VALUES ('070200', '9999', 80)
GO
```

期望：两次都 **失败**，`INSERT 语句与 FOREIGN KEY 约束冲突`

#### ③ 满足约束：外码取值在被参照主码范围内

```sql
USE StudentCourse1
GO
INSERT INTO StuCourse VALUES ('070200', '2001', 85)
GO
SELECT * FROM StuCourse WHERE 学号 = '070200' AND 课程号 = '2001'
GO
```

期望：**成功**，查询到 `070200 2001 85`

---

## 三、用户定义完整性

### (3) NOT NULL 约束

> 以 Student1 表的"姓名"列为例（NOT NULL 且无默认值）。

#### ① 违反约束：插入 NULL

```sql
USE StudentCourse1
GO
INSERT INTO Student1 (学号, 姓名, 性别, 出生时间, 总学分)
VALUES ('070201', NULL, '男', '2000-01-01', 10)
GO
```

期望：**失败**，`不能将值 NULL 插入列 '姓名'`

#### ② 满足约束：插入非 NULL 值

```sql
USE StudentCourse1
GO
INSERT INTO Student1 (学号, 姓名, 性别, 出生时间, 总学分)
VALUES ('070201', '正常姓名', '女', '2000-01-01', 10)
GO
SELECT * FROM Student1 WHERE 学号 = '070201'
GO
```

期望：**成功**，查询到 `070201 正常姓名 女`

---

### (4) UNIQUE 约束

> 以 Student1 表的"姓名"列为例（约束名 sname_unique）。

#### ① 违反约束：插入重复姓名

```sql
USE StudentCourse1
GO
INSERT INTO Student1 (学号, 姓名, 性别, 出生时间, 总学分)
VALUES ('070202', '测试学生', '男', '2000-03-03', 15)
GO
```

期望：**失败**，`违反了 UNIQUE KEY 约束 'sname_unique'`（"测试学生"已存在）

#### ② 满足约束：插入不重复的姓名

```sql
USE StudentCourse1
GO
INSERT INTO Student1 (学号, 姓名, 性别, 出生时间, 总学分)
VALUES ('070202', '不重复的姓名', '男', '2000-03-03', 15)
GO
SELECT * FROM Student1 WHERE 学号 = '070202'
GO
```

期望：**成功**，查询到 `070202 不重复的姓名 男`

---

### (5) DEFAULT 约束

> 以 Student1 表的"性别"列为例（默认值 '男'）。

#### ① 给定具体属性值的情况

```sql
USE StudentCourse1
GO
INSERT INTO Student1 (学号, 姓名, 性别, 出生时间, 总学分)
VALUES ('070203', '指定性别', '女', '2000-05-05', 12)
GO
SELECT 学号, 姓名, 性别 FROM Student1 WHERE 学号 = '070203'
GO
```

期望：**成功**，性别显示为 **女**（指定的值覆盖了默认值）

#### ② 没有给定具体属性值的情况

```sql
USE StudentCourse1
GO
INSERT INTO Student1 (学号, 姓名, 出生时间, 总学分)
VALUES ('070204', '默认性别', '2000-06-06', 12)
GO
SELECT 学号, 姓名, 性别 FROM Student1 WHERE 学号 = '070204'
GO
```

期望：**成功**，性别显示为 **男**（默认值生效）

---

### (6) CHECK 约束

> 以 Course 表"开课学期"为例（Sem_Check: 1~8），以及 Student1 表"总学分"为例（tot_Credit_Check: >0）。

#### ① 违反约束的情况

```sql
USE StudentCourse1
GO
-- CHECK-总学分<=0
INSERT INTO Student1 (学号, 姓名, 性别, 出生时间, 总学分)
VALUES ('070205', '负学分', '男', '2000-05-05', -5)
GO
```

```sql
USE StudentCourse1
GO
-- CHECK-成绩>100
INSERT INTO StuCourse VALUES ('070200', '2001', 101)
GO
```

```sql
USE StudentCourse1
GO
-- CHECK-性别非法
INSERT INTO Student1 (学号, 姓名, 性别, 出生时间, 总学分)
VALUES ('070206', '性别测试', '未知', '2000-06-06', 10)
GO
```

期望：三次都 **失败**，`INSERT 语句与 CHECK 约束冲突`

#### ② 满足约束的情况

```sql
USE StudentCourse1
GO
-- 总学分>0，性别合法
INSERT INTO Student1 (学号, 姓名, 性别, 出生时间, 总学分)
VALUES ('070205', '正学分', '男', '2000-05-05', 18)
GO
SELECT * FROM Student1 WHERE 学号 = '070205'
GO
```

```sql
USE StudentCourse1
GO
-- 成绩在0~100内
INSERT INTO StuCourse VALUES ('070200', '2001', 88)
GO
SELECT * FROM StuCourse WHERE 学号 = '070200' AND 课程号 = '2001'
GO
```

期望：两次都 **成功**，数据正常插入

---

## 题目4：完成教材附录实验 10（数据库安全性）

> 先确认 SQL Server 已设为"SQL Server 和 Windows 身份验证模式"。

### 步骤 1 — 创建登录名

```sql
IF NOT EXISTS (SELECT name FROM sys.server_principals WHERE name = 'sutdLogin')
    CREATE LOGIN sutdLogin WITH PASSWORD = '123'
GO
IF NOT EXISTS (SELECT name FROM sys.server_principals WHERE name = 'sutdLogin2')
    CREATE LOGIN sutdLogin2 WITH PASSWORD = '123'
GO
```

期望：两个登录名创建成功

### 步骤 2 — 创建数据库用户

```sql
USE StudentCourse
GO
IF NOT EXISTS (SELECT name FROM sys.database_principals WHERE name = 'sutdUsr')
    CREATE USER sutdUsr FOR LOGIN sutdLogin
GO
IF NOT EXISTS (SELECT name FROM sys.database_principals WHERE name = 'sutdUsr2')
    CREATE USER sutdUsr2 FOR LOGIN sutdLogin2
GO
```

期望：两个数据库用户创建成功

### 步骤 3 — 授予 SELECT 权限并验证

```sql
USE StudentCourse
GO
GRANT SELECT ON Student TO sutdUsr, sutdUsr2
GO

EXECUTE AS USER = 'sutdUsr'
SELECT * FROM Student
REVERT
GO
```

期望：成功查询出 Student 表全部数据

### 步骤 4 — 授予 INSERT 权限（含 GRANT OPTION）

```sql
USE StudentCourse
GO
GRANT INSERT ON Student TO sutdUsr WITH GRANT OPTION
GO
```

期望：成功

### 步骤 5 — sutdUsr 将 INSERT 权限授予 sutdUsr2

```sql
USE StudentCourse
GO
EXECUTE AS USER = 'sutdUsr'
GRANT INSERT ON Student TO sutdUsr2
REVERT
GO
```

期望：成功（因为 sutdUsr 有 WITH GRANT OPTION）

### 步骤 6 — sutdUsr2 插入数据验证

```sql
USE StudentCourse
GO
EXECUTE AS USER = 'sutdUsr2'
INSERT INTO Student(学号, 姓名, 性别, 出生时间, 总学分, 备注)
VALUES ('070115', '李小明', '女', getdate(), 10, '三好学生')
REVERT
GO

SELECT * FROM Student WHERE 学号 = '070115'
GO
```

期望：**成功插入并查询到 `070115 李小明 女 ... 10 三好学生`**

### 步骤 7 — CASCADE 级联回收 INSERT

```sql
USE StudentCourse
GO

REVOKE INSERT ON Student FROM sutdUsr CASCADE
GO

-- 验证 sutdUsr2 不能再插入
BEGIN TRY
    EXECUTE AS USER = 'sutdUsr2'
    INSERT INTO Student(学号, 姓名, 性别, 出生时间, 总学分)
    VALUES ('070116', '张测试', '男', getdate(), 10)
    REVERT
    PRINT '插入成功 — 权限回收未生效!'
END TRY
BEGIN CATCH
    PRINT '权限验证通过 — 插入被拒绝: ' + ERROR_MESSAGE()
    REVERT
END CATCH
GO

DELETE FROM Student WHERE 学号 = '070115'
GO
```

期望：输出 **`权限验证通过 — 插入被拒绝`**，证明 CASCADE 回收生效。

---

## 题目5：思考与练习（级联回收 UPDATE、DELETE）

### 练习1 — 授予 sutdUsr UPDATE、DELETE 权限（含 GRANT OPTION）

```sql
USE StudentCourse
GO
GRANT UPDATE, DELETE ON Student TO sutdUsr WITH GRANT OPTION
GO
```

期望：成功

### 练习2 — sutdUsr 将权限传递给 sutdUsr2，并验证

```sql
USE StudentCourse
GO

EXECUTE AS USER = 'sutdUsr'
GRANT UPDATE, DELETE ON Student TO sutdUsr2
REVERT
GO

-- sutdUsr2 修改数据验证（应成功）
BEGIN TRY
    EXECUTE AS USER = 'sutdUsr2'
    UPDATE Student SET 备注 = '权限传递成功' WHERE 学号 = '070110'
    REVERT
    PRINT '权限传递验证通过 — UPDATE 执行成功'
END TRY
BEGIN CATCH
    PRINT '权限传递失败: ' + ERROR_MESSAGE()
    REVERT
END CATCH
GO

SELECT 学号, 姓名, 备注 FROM Student WHERE 学号 = '070110'
GO

-- 恢复
UPDATE Student SET 备注 = NULL WHERE 学号 = '070110'
GO
```

期望：输出 **`权限传递验证通过 — UPDATE 执行成功`**，备注列显示 **权限传递成功**

### 练习3 — CASCADE 级联回收，验证回收成功

```sql
USE StudentCourse
GO

REVOKE UPDATE, DELETE ON Student FROM sutdUsr CASCADE
GO

-- sutdUsr2 再尝试修改（应失败）
BEGIN TRY
    EXECUTE AS USER = 'sutdUsr2'
    UPDATE Student SET 备注 = '应失败' WHERE 学号 = '070110'
    REVERT
    PRINT '修改成功 — 权限回收未生效!'
END TRY
BEGIN CATCH
    PRINT '权限回收验证通过 — 修改被拒绝: ' + ERROR_MESSAGE()
    REVERT
END CATCH
GO
```

期望：输出 **`权限回收验证通过 — 修改被拒绝`**，证明 CASCADE 回收生效

---

## 汇总

| 题目 | 执行次数 | 成功 | 失败（截图用） |
|------|:--------:|:----:|:-------------:|
| 题目1 建库建表 | 1 批 | ✓ | — |
| 题目2 修改约束 | 1 批 | ✓ 学时=0；✗ 学时=121 | 1 个错误截图 |
| 题目2 触发器 | 1 批 | ✓ 第1次INSERT；✗ 第2次INSERT | 1 个错误截图 |
| 题目3 PRIMARY KEY ①主码重复 | 1 条 | — | 1 个错误截图 |
| 题目3 PRIMARY KEY ②主码为空 | 1 条 | — | 1 个错误截图 |
| 题目3 PRIMARY KEY ③满足约束 | 1 条 | ✓ 插入成功 | — |
| 题目3 FOREIGN KEY ①外码NULL | 1 条 | ✓ 成绩NULL | — |
| 题目3 FOREIGN KEY ②违反(2条) | 2 条 | — | 2 个错误截图 |
| 题目3 FOREIGN KEY ③满足约束 | 1 条 | ✓ 插入成功 | — |
| 题目3 NOT NULL ①违反 | 1 条 | — | 1 个错误截图 |
| 题目3 NOT NULL ②满足 | 1 条 | ✓ 插入成功 | — |
| 题目3 UNIQUE ①违反 | 1 条 | — | 1 个错误截图 |
| 题目3 UNIQUE ②满足 | 1 条 | ✓ 插入成功 | — |
| 题目3 DEFAULT ①给定值 | 1 条 | ✓ 显示'女' | — |
| 题目3 DEFAULT ②未给定 | 1 条 | ✓ 显示'男'(默认) | — |
| 题目3 CHECK ①违反(3条) | 3 条 | — | 3 个错误截图 |
| 题目3 CHECK ②满足(2条) | 2 条 | ✓ 插入成功 | — |
| 题目4 实验10 | 7 批 | ✓ 各步骤成功 | 1 个错误截图（回收验证） |
| 题目5 级联回收 | 3 批 | ✓ 授权+传递 | 1 个错误截图（回收验证） |
