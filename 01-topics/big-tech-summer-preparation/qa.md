# 求职准备问题库

## 定位问题

- 为什么你的主定位是 Java 后端，而实习技术栈主要是 TypeScript？
- Agent 工程能力与普通 LLM API 调用的区别是什么？
- 你希望加入大厂后解决哪一类问题？

## Agent 实习深挖

- Memory 的读、写、召回、作用域和写回策略分别如何设计？
- 插件 SDK 与宿主如何解耦？版本不兼容怎么办？
- 外部 IM 消息如何映射到平台用户、租户、会话和运行实例？
- 多实例竞争的根因是什么？Bull 队列方案保证了什么，没保证什么？
- 你个人负责的边界、最难的问题和可验证结果分别是什么？

## Java 并发

- volatile 能解决什么，不能解决什么？
- synchronized 和 ReentrantLock 如何选择？
- 线程池为什么不能直接照搬固定公式？
- CompletableFuture 默认线程池有哪些风险？
- 如何实现任务取消、超时、背压和优雅停机？
- 虚拟线程适合 Agent 工具调用吗？它不能替代什么？

## 后端可靠性

- 重试为什么可能放大故障？
- 如何保证外部 API 调用的幂等？
- 进程在任务执行一半时退出，如何恢复？
- 数据库事务与外部副作用无法放在同一事务时怎么办？
- 限流、隔离、熔断和降级分别解决什么问题？

## 项目复盘

- 如果用户量增长十倍，CampusBites 最先出现的瓶颈是什么？
- 微信素材批量上传为什么使用 CompletableFuture？替代方案是什么？
- 哪个设计现在看来是过度设计或不足设计？
- 如何证明性能提升，而不是凭感觉描述？

