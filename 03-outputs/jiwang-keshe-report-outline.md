# 计算机网络课程设计报告起草建议

适用对象：

- 课程要求：`中小型网络工程设计与实现` + `基于 Socket 的局域网通信软件的设计与实现`
- 你的项目来源：
  - 网络设计要求文档：[课程设计大纲、要求、格式、封面.docx](</D:/qq_setup_31980/课程设计大纲、要求、格式、封面.docx>)
  - 聊天软件仓库：[rainhotel/jiwang-keshe](https://github.com/rainhotel/jiwang-keshe)

这份文档的目标不是直接代替最终报告，而是告诉你“这一份课设报告最合适怎么写”，并给出一版贴近你项目实际情况的正文起草方向。

---

## 一、先确定整份报告的写法

老师的要求很明确：**两个内容都要做，而且最好放在同一份课程设计报告里**。  
所以这份报告最合适的结构不是“只写网络”或“只写聊天软件”，而是：

1. 封面
2. 题目、作者、专业班级
3. 摘要
4. 关键词
5. 第一章：项目背景与课程设计目标
6. 第二章：中小型网络工程设计与实现
7. 第三章：基于 Socket 的局域网聊天软件设计与实现
8. 第四章：总结与心得
9. 参考文献

这样写的好处有三个：

1. 完全贴合课程要求，不会被认为“少做一部分”。
2. 结构清晰，老师一看就知道你完成了哪两块内容。
3. 网络部分和软件部分既独立，又能体现你对“基础设施 + 网络应用”的完整理解。

---

## 二、题目怎么起更合适

如果老师允许总标题自己定，我建议用下面这个版本：

**《计算机网络课程设计：校园网络工程设计与局域网聊天软件实现》**

如果你想更正式一点，也可以用：

**《基于 Packet Tracer 的校园网络设计与基于 Socket 的局域网聊天软件设计与实现》**

这两个标题都比“计网课设报告”更具体，也能直接反映你做了哪两部分内容。

---

## 三、摘要怎么写最合适

你的摘要不要分成两小段分别写，而要写成一个整体，体现“同一门课设的两个实践任务”。

可以按这个思路写：

> 本课程设计围绕计算机网络的基础设施建设与网络应用开发两个方面展开，完成了两项实践任务：一是基于 Packet Tracer 平台的校园中小型网络工程设计与实现，二是基于 Socket 的局域网聊天软件设计与实现。网络工程部分对校园场景进行了需求分析，完成了网络拓扑规划、VLAN 划分、地址设计、核心交换与路由配置，并部署了 DNS、WWW 等基础服务，实现了内外网连通。聊天软件部分采用 C/S 架构，基于 Python 的 socket、threading 和 tkinter 等技术实现了用户注册登录、公聊、私聊、聊天记录管理、群聊、联系人与文件传输等功能。通过本次课程设计，加深了对 TCP/IP 协议、交换路由技术以及 Socket 网络编程原理的理解，提高了综合运用理论知识解决实际问题的能力。

关键词建议写：

`Packet Tracer；VLAN；静态路由；Socket；局域网聊天；C/S 架构`

---

## 四、整份正文最推荐的结构

---

## 第一章 项目背景与课程设计目标

这一章不要写太长，主要起总领作用。建议分成 3 节：

### 1.1 项目背景

可以写：

- 计算机网络课程不仅要求掌握理论，还要求具备网络设计和应用开发能力。
- 现代校园网络既需要稳定的基础设施，也需要支撑上层应用。
- 因此，本次课程设计从“网络工程”和“网络应用软件”两个方面展开。

### 1.2 课程设计目的

这里可以直接贴合老师要求：

- 掌握中小型网络规划与实现方法
- 熟悉 VLAN、交换、路由、基础网络服务配置
- 理解 Socket API 的工作机制
- 掌握局域网聊天系统的基本设计与实现流程

### 1.3 本报告的主要内容

这节直接概括：

- 第二章写校园网络拓扑设计与实现
- 第三章写局域网聊天软件设计与实现
- 最后做总结与心得

---

## 第二章 中小型网络工程设计与实现

这章一定要完全按老师给的四部分来写：

1. 需求分析
2. 概要设计
3. 详细设计
4. 调试分析

### 2.1 需求分析

这里要写“校园场景对网络的需求”，建议你写成你自己这次实际做的版本，不要照仓库里那个“智慧校园六区 + DHCP + NAT + ACL”的老模板全抄，因为你这次真实做出来的是：

- 多 VLAN 校园网
- 两台核心交换机
- 多台接入交换机
- 多台核心路由器/出口路由器
- DNS 和 WWW 服务
- 校园网访问右侧外网

最适合的写法是：

#### 2.1.1 场景描述

本设计以校园网络为背景，网络中包含实验室、教学楼、图书馆、服务器区等多个功能区域。由于不同区域承担的业务不同，需要对网络进行逻辑划分和统一管理，并支持校内资源访问以及与外部网络互联。

#### 2.1.2 功能需求

可以列这些：

1. 不同区域应划分到不同 VLAN 中，实现广播域隔离。
2. 校园内部不同 VLAN 之间应能实现互通。
3. 服务器区应部署 DNS 和 WWW 服务。
4. 校园内部用户应能够访问外网主机和外网服务器。
5. 核心层应具备一定冗余结构，提高网络可靠性。

#### 2.1.3 技术需求

这里写：

- VLAN 划分
- Trunk 中继
- 三层交换
- 静态路由
- DNS 和 Web 服务部署

#### 2.1.4 地址与设备需求

这里建议放一张表：

| 项目 | 内容 |
|---|---|
| 核心交换机 | 2 台 3560 |
| 接入交换机 | 4 台 2960 |
| 核心/出口路由器 | 多台 2811 |
| 服务器 | DNS 服务器、校园 Web 服务器、外网 Web 服务器 |
| 客户端 | 内网 PC、外网 PC |

---

### 2.2 概要设计

这部分老师要求你“画出网络拓扑结构图，说明主要功能实现过程”。  
你这章最关键的是 **拓扑图 + VLAN/IP 规划表 + 功能说明**。

建议分成下面几节：

#### 2.2.1 网络拓扑结构

这里放你 Packet Tracer 的完整拓扑截图。

截图说明建议写：

> 图中左侧为校园内部网络，包含服务器区、实验室、教学楼和图书馆等业务区域；中间为核心交换和核心路由部分；右侧为外部网络区域。校园网通过核心交换机实现 VLAN 间转发，通过核心路由器和出口路由器与外部网络连接。

#### 2.2.2 VLAN 规划

这一节建议写成表格，你这次可以按真实配置写：

| VLAN | 区域 | 网段 | 网关 |
|---|---|---|---|
| VLAN10 | 服务器区 | 192.168.10.0/24 | 192.168.10.254 |
| VLAN20 | 实验室 | 192.168.20.0/24 | 192.168.20.254 |
| VLAN30 | 教学楼 | 192.168.30.0/24 | 192.168.30.254 |
| VLAN40 | 图书馆 | 192.168.40.0/24 | 192.168.40.254 |

#### 2.2.3 地址规划

把核心路由和外部网络也列出来：

| 链路/区域 | 地址规划 |
|---|---|
| CORE-SW1 与 CORE-R1 | 192.168.254.0/24 |
| CORE-R1 与 CK-Router | 10.0.12.0/30 |
| CK-Router 与 P-Router | 200.10.1.0/28 |
| 外网用户区 | 100.10.1.0/28 |

如果你最后把冗余链路也配好了，还可以补：

- `CORE-R1 <-> CORE-R2`
- `CK-Router <-> CORE-R2`
- `CORE-SW1 <-> CORE-R2`

#### 2.2.4 服务部署方案

建议写：

| 服务 | 设备 | IP 地址 | 作用 |
|---|---|---|---|
| DNS | DNS-Server | 192.168.10.2 | 域名解析 |
| WWW | www.niust.edu | 192.168.10.3 | 校园网站服务 |
| WWW | www.baidu.com | 100.10.1.3 | 外网服务器模拟 |

#### 2.2.5 主要功能实现过程

这里用几段短文字解释：

- 通过核心交换机创建 VLAN 并配置 SVI 作为各 VLAN 网关
- 通过 Trunk 连接核心交换机与接入交换机
- 通过静态路由打通校园网和外部网络
- 通过 DNS 服务支持域名解析
- 通过 Web 服务器验证应用层访问

---

### 2.3 详细设计

这一节是老师最看重的“核心交换机和路由器配置文件清单”。

但注意，不要把全部命令毫无组织地堆上去。  
最好的写法是“按设备分类，每类设备给关键配置 + 简短解释”。

建议结构：

#### 2.3.1 核心交换机配置

这里写：

- VLAN 创建
- SVI 网关配置
- `ip routing`
- Trunk 配置

你可以放：

- `CORE-SW1` 关键命令
- `CORE-SW2` 关键命令

并且每段命令后解释：

- 输入是什么
- 实现了什么
- 作用在哪一层

#### 2.3.2 接入交换机配置

这里写 4 台接入交换机的共同模式：

- 上联口为 Trunk
- 下联口为 Access
- 按不同 VLAN 加入对应区域

#### 2.3.3 核心路由器和出口路由器配置

建议按以下设备分别写：

- `CORE-Router 1`
- `CK-Router`
- `P-Router`
- 如果你最后保留冗余配置，再写 `CORE-Router 2`

重点写：

- 接口 IP 配置
- 静态路由配置
- 串口配置
- 默认路由和返回路由

#### 2.3.4 服务器与主机配置

这里写：

- DNS 服务器 IP、网关、DNS 服务记录
- Web 服务器 IP 和 HTTP 服务
- 外网 PC 和外网服务器 IP、网关

这一节后面特别适合插截图。

---

### 2.4 调试分析

这一节要写“测试过程 + 结果 + 问题与解决”。  
你这次实际调试过程很丰富，这反而是报告亮点。

建议分成几小节：

#### 2.4.1 VLAN 与网关测试

写：

- VLAN20 主机 ping `192.168.20.254` 成功
- VLAN30 主机 ping `192.168.30.254` 成功
- VLAN40 主机 ping `192.168.40.254` 成功

#### 2.4.2 跨 VLAN 通信测试

写：

- 内网主机可以 ping `192.168.10.2`
- 说明核心交换机已实现跨 VLAN 三层通信

#### 2.4.3 内外网互通测试

写：

- 左侧内网 PC 可以 ping `100.10.1.1`
- 可以 ping `100.10.1.2`
- 可以 ping `100.10.1.3`

#### 2.4.4 调试过程中遇到的问题

这一段非常适合写真实经历，你这次至少可以写这几个：

1. `switchport mode trunk` 报封装错误，需要先配置 `dot1q`
2. 部分端口忘记 `no shutdown`
3. 路由器串口接口不存在，需要安装 `WIC-2T`
4. 右侧网关口一直 `down/down`，最后通过重新插线解决
5. 左边 PC 最终无法访问右边主机，是因为 `CORE-R1` 缺少返回校园网的静态路由

这一节会让老师看出你不是“照抄配置”，而是真正做了调试。

---

## 第三章 基于 Socket 的局域网聊天软件设计与实现

这一章同样按老师要求写 4 部分：

1. 需求分析
2. 概要设计
3. 详细设计
4. 实现

而且这一章一定要以 **你仓库的真实实现** 为准。  
我看了你的仓库，聊天软件部分现在的实际情况比单纯“注册/登录/公聊/私聊”更丰富，应该按真实功能写。

---

### 3.1 需求分析

这里建议按“功能需求 + 非功能需求”来写。

#### 3.1.1 功能需求

结合你的代码，建议列成表：

| 编号 | 功能 | 说明 |
|---|---|---|
| F1 | 用户注册 | 新用户可创建账号 |
| F2 | 用户登录 | 已注册用户登录系统 |
| F3 | 公共聊天 | 所有在线用户可接收广播消息 |
| F4 | 私聊 | 用户可向指定对象发送私聊消息 |
| F5 | 历史消息查看 | 支持查看公共和私聊历史记录 |
| F6 | 群聊 | 支持创建群、加群、退群、群消息发送 |
| F7 | 联系人管理 | 支持添加联系人 |
| F8 | 用户搜索 | 支持按名称搜索用户 |
| F9 | 文件传输 | 支持文件发送与下载相关处理 |
| F10 | 在线用户列表 | 客户端可查看当前在线用户 |

这里要注意：  
你的 README 还偏简单，但从代码看，**群聊、联系人、搜索用户、文件功能都已经进了协议和数据库设计**，所以报告里写这些更合理。

#### 3.1.2 非功能需求

可以写：

- 支持多客户端并发
- 界面简单易用
- 聊天记录可持久化
- 断线后能够提示用户
- 使用标准库实现，便于部署

#### 3.1.3 技术选型

你的真实实现建议这样写：

| 技术 | 用途 |
|---|---|
| Python | 主开发语言 |
| socket | TCP 网络通信 |
| threading | 并发处理与后台接收 |
| tkinter | 图形界面 |
| json | 消息封装 |
| SQLite | 用户、消息、群组、文件等数据持久化 |

这里要特别注意：  
**不要再写 users.json 持久化**。  
你的实际实现已经是 `chat.db`，而且 `server/database.py` 明确用了 SQLite 和 WAL 模式。

---

### 3.2 概要设计

这一节写“系统整体结构”和“通信协议”最合适。

#### 3.2.1 系统结构设计

可以明确写成 **C/S 架构**：

- 服务端负责用户管理、消息转发、聊天记录保存、群组与文件元数据管理
- 客户端负责图形界面、消息输入展示、网络收发

你可以放一个系统结构图：

- 左边多个 Client
- 中间 Server
- 下方 SQLite 数据库

#### 3.2.2 模块划分

根据仓库实际结构写：

- `server/main.py`：服务端入口
- `server/server.py`：服务端核心运行逻辑
- `server/handlers.py`：消息处理器
- `server/database.py`：数据库层
- `client/main.py`：客户端入口
- `client/network.py`：客户端网络层
- `client/ui/*.py`：界面模块
- `common/protocol.py`：协议常量与消息封装

这一节写出来会很像正式的软件系统设计。

#### 3.2.3 通信协议设计

这一节非常值得写，也是网络软件的“专业感”来源。

你可以写：

- 系统采用 TCP 协议通信
- 消息采用 JSON 编码
- 每条消息包含 `type` 字段

然后列几类消息：

| 类型 | 作用 |
|---|---|
| `register` | 注册 |
| `login` | 登录 |
| `broadcast` | 公聊 |
| `private` | 私聊 |
| `get_users` | 获取在线用户 |
| `get_history` | 获取历史消息 |
| `create_group` | 创建群聊 |
| `join_group` | 加入群聊 |
| `group_msg` | 群消息 |
| `file_send` | 文件发送 |
| `file_download` | 文件下载 |

这里最好配一张“通信流程图”或者“登录时序图”。

---

### 3.3 详细设计

这一章老师要求“类图、序列图、算法流程图或伪代码”。  
你不一定必须画 UML 到非常标准，但至少要有“模块功能 + 核心流程”。

推荐写法：

#### 3.3.1 服务端设计

这里基于你的真实代码写：

- 服务端启动时初始化数据库
- 监听指定地址和端口
- 为每个客户端连接创建处理流程
- 根据消息 `type` 分发给不同处理函数

重点可以分析 `server/handlers.py`：

- `handle_register`
- `handle_login`
- `handle_broadcast`
- `handle_private`
- `handle_get_history`
- `handle_create_group`
- `handle_group_msg`
- `handle_file_send`

这一节非常适合放一段“消息分发流程伪代码”。

#### 3.3.2 数据库设计

这是你项目里很值得写的一部分，因为实现比普通课设更完整。

从 `server/database.py` 看，你至少有这些表：

- `users`
- `messages`
- `groups`
- `group_members`
- `contacts`
- `files`

你应该把它们写成表格：

| 表名 | 作用 |
|---|---|
| users | 保存用户账号和密码 |
| messages | 保存聊天记录 |
| groups | 保存群组信息 |
| group_members | 保存群成员关系 |
| contacts | 保存联系人 |
| files | 保存文件传输元数据 |

然后挑 2 到 3 张最关键的表结构截图或简化说明图放进去。

#### 3.3.3 客户端设计

这一节写：

- `NetworkClient` 负责连接、发送、接收、回调分发
- `tkinter` 界面负责登录、聊天、用户列表等显示
- 接收线程在后台循环读取服务器消息，并根据消息类型调用对应处理函数

你可以特别写一下 `client/network.py` 中的思路：

- `connect()`
- `send()`
- `send_and_wait()`
- `start_receive()`
- `_recv_loop()`

#### 3.3.4 关键算法或流程

推荐至少写两个流程：

1. 用户登录流程
2. 私聊/群聊消息发送流程

如果你愿意多写一点，还可以加：

3. 历史消息拉取流程
4. 文件发送登记流程

---

### 3.4 实现与运行结果

这一节不要空泛，要多放截图，而且每张图后面都要解释。

最适合放的截图有：

1. 登录界面
2. 注册成功界面
3. 公聊界面
4. 私聊界面
5. 在线用户列表
6. 群聊相关功能
7. 文件传输或下载界面
8. 服务端运行终端截图

关键代码建议不要贴太长，挑这些：

- 协议封装代码
- 登录处理代码
- 公聊/私聊处理代码
- 数据库建表或消息存储代码
- 客户端接收线程代码

每段代码后说明：

- 输入是什么
- 做了什么处理
- 输出或效果是什么

---

## 第四章 总结与心得

这一章不要只写“我学到了很多”。  
最好分成两部分：

### 4.1 网络工程部分的收获

可以写：

- 理解了 VLAN、Trunk、三层交换和静态路由的配合方式
- 学会了如何从链路状态、接口状态、路由表三个角度排查网络问题
- 理解了内网访问外网时“去程路由”和“回程路由”都必须完整

### 4.2 聊天软件部分的收获

可以写：

- 理解了 TCP 长连接和 C/S 架构
- 学会了使用多线程处理多客户端
- 理解了协议设计、消息分发、界面与网络解耦的思路
- 学会了使用 SQLite 持久化聊天数据

### 4.3 综合收获

这一段建议点明“从网络到应用”的完整链条：

- 网络基础设施保证数据传输
- Socket 软件体现应用层功能
- 两部分结合体现了计算机网络课程从底层到上层的完整知识体系

---

## 五、你现在这份报告最需要注意的地方

### 1. 不要直接照搬仓库里现有 `report/report.md`

原因：

- 里面网络部分写的是另一套更理想化的 DHCP/NAT/ACL 校园网
- 你这次实际搭的 Packet Tracer 拓扑和它不完全一样
- 你聊天软件 README 写得比实际代码简单，容易把 SQLite 写漏

所以最好的做法是：

- 网络部分按你这次真实拓扑写
- 聊天软件部分按当前仓库代码真实功能写

### 2. 图、表、代码后面一定加解释

老师已经明确写了这一点，所以你的报告里不要只是“放图”。  
每张图后至少写 2 到 4 句说明。

### 3. 网络部分不要只贴命令

要说明：

- 为什么这样配
- 这段配置属于哪台设备
- 它实现了什么功能

### 4. 聊天软件部分一定要突出“协议设计”和“数据库设计”

因为这两点最能体现你不是只做了个简单 GUI，而是真做了一个有结构的网络程序。

---

## 六、最推荐的最终写作策略

如果你要最高效地完成这份报告，我建议这样做：

1. 先用这份结构作为总目录
2. 网络部分优先补：
   - 完整拓扑图
   - VLAN/IP 规划表
   - 核心配置截图
   - ping 测试截图
3. 聊天软件部分优先补：
   - 系统结构图
   - 登录/聊天界面截图
   - 关键代码截图
   - 数据库表说明
4. 最后统一调整版式到老师要求

---

## 七、我建议你下一步怎么配合我

如果你愿意，我最建议下一步直接这样继续：

1. 你把 **Packet Tracer 拓扑图** 和 **聊天室运行截图** 发给我
2. 我按这份结构，继续帮你把正文扩成“可直接交”的版本
3. 我再顺手帮你把：
   - 摘要
   - 第二章正文
   - 第三章正文
   - 总结
   统一润色成一份更正式的课程设计文风

如果你想更快一点，我也可以下一步直接帮你：

- 先写一版 **完整报告初稿**
- 然后把需要你补截图的位置全部标成 `[此处插入截图]`
