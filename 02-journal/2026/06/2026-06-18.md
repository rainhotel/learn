# 2026-06-18

## 计算机组成课程设计：单总线 CPU

- 阅读课程设计任务书，确认选择的是第 1、3 个处理器实验：
  - 实验 5-1：单总线 CPU 设计，定长指令周期 3 级时序。
  - 实验 5-3：单总线 CPU 设计，现代时序。
- 定位本地资料包：`hustzc\7.单总线CPU\单总线实验资料包(愚人节版)`。
- 解析 Logisim `.circ` 文件，发现：
  - `MipsOnBusCpu-3-exp5-1.circ` 已含实验 5-1 参考完成电路。
  - `MipsOnBusCpu-1.circ` 的现代时序微程序 ROM 仍为空，需要自行补微程序。
- 新建主题目录：`01-topics/computer-organization-course-design`。
- 整理了实验 5-1、5-3 的接线和微程序设计指南。

## 高可用 AI API 网关调研

- 发起研究方向：`06-research/ai-api-gateway-ha-design`。
- 检索 GitHub 上的 AI Gateway / LLM Gateway / OpenAI-compatible gateway 项目。
- 初步覆盖：
  - LLM 专用网关：LiteLLM、Portkey Gateway、Bifrost、Helicone AI Gateway、GoModel、AxonHub、new-api。
  - API 网关 AI 化：Apache APISIX、Higress、Envoy AI Gateway。
  - LLMOps 平台：TensorZero。
- 初步结论：
  - 简历项目不要只做简单 OpenAI proxy。
  - 更适合做“OpenAI-compatible + 多 provider 路由 + fallback + 熔断 + Redis 分布式限流 + Prometheus 观测 + 故障注入压测”的高可用网关。

## 高含金量简历项目收束

- 将简历项目收束为：`ModelMesh Gateway`，高可用 AI API 网关。
- 推荐技术栈：Java 21 + Spring Boot WebFlux + Redis + PostgreSQL + Prometheus/Grafana。
- 新增项目计划：`06-research/ai-api-gateway-ha-design/resume-project-plan.md`。
- 项目核心卖点：
  - OpenAI-compatible Chat Completions。
  - SSE 流式透传。
  - 多 Provider 权重路由、fallback、熔断。
  - Redis 分布式 RPM/TPM 限流。
  - Mock Provider 故障注入和压测报告。
