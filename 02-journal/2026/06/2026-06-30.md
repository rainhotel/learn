# 2026-06-30

## Algorithm Final Review

- 新增 `01-topics/algorithm-final-review/cpp98-exam-template-sheet.md`。
- 内容覆盖：`STL` 常用语法、`pair/make_pair`、非 `vector` 的 `DFS/BFS`、贪心策略、常见 DP 状态转移、`0/1` 背包、完全背包、`Dijkstra`、`Prim`、`lower_bound/upper_bound`、普通二分、二分答案。
- 约束：全部按 `C++98` 风格整理，人类可读性优先，板子以能默写和能改题为目标。
- 生成 `01-topics/algorithm-final-review/cpp98-exam-template-guide.docx`，补充重要算法的变量说明、核心循环解释和易错点。
- 修订 `cpp98-exam-template-guide.docx` 与 `cpp98-exam-template-sheet.md`：扩充 `STL` 语法，增加经典题型拓展和边界处理说明，简化 `Dijkstra` 板子以提高可读性。
- 继续补充 `lower_bound / upper_bound`：加入基本语法、数组和 `vector` 用法、存在性判断、出现次数、前驱后继、`pair` 用法、`set` 用法、降序比较器语法和边界提醒。

## Next

- 手写一遍 `BFS`、`0/1` 背包、完全背包、`Dijkstra`、`Prim`。
- 每个板子至少配 1 道题，后续沉淀到 `solved-problems.md`。

## Milvus Open Source Contribution

- 选定第一个贡献目标：`milvus-io/milvus-docs#3481`，澄清 `list_collections()` 与 `has_collection()` 的使用场景。
- 在临时工作副本 `codex-tmp/milvus-docs-3481` 中完成文档修改。
- 修改文件：`site/en/userGuide/collections/view-collections.md`。
- 改动内容：在 `List Collections` 示例结果后增加 note，说明：
  - `list_collections()` 用于获取当前数据库中的全部 collection 名称；
  - 检查某个 collection 是否存在时，应使用 `client.has_collection(collection_name="quick_setup")`。
- 本地检查：`git diff --check` 通过；改动范围为 1 个 Markdown 文件、8 行新增。
- 下一步：fork `milvus-io/milvus-docs`，把本地分支推到自己的 fork，并提交 PR。
